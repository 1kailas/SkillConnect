# Server Configuration
NODE_ENV=development
PORT=5000

# MongoDB - Add your database name at the end
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/skillconnect?retryWrites=true&w=majority
# For local MongoDB:
# MONGODB_URI=mongodb://localhost:27017/skillconnect

# Frontend URL
FRONTEND_URL=http://localhost:5173

# JWT Secret - REQUIRED, MUST be at least 32 characters for security
# Generate a secure secret with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# NEVER commit your actual JWT_SECRET to version control!
JWT_SECRET=your_super_secure_jwt_secret_must_be_at_least_32_characters_long_here

# Groq AI API - REQUIRED for AI features
# Get your API key from: https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here

# Rate Limiting (optional - defaults provided)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Email Configuration (optional)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
